import 'package:flutter/material.dart';

class AppColors {
  // Primary 1: Dark Purple (Text, Inactive Icons, Headers)
  static const Color primary = Color(0xFF4A154B); // Deep Purple
  static const Color primaryDark = Color(0xFF2A082C);

  // Primary 2 (Accent): Lime Green (Buttons, Active Icons, Highlight)
  static const Color accent = Color(0xFFC6FF00); // Lime/Light Green
  static const Color accentDark = Color(0xFFAEEA00);

  // Neutrals
  static const Color background = Colors.white;
  static const Color textDark = Color(0xFF333333);
  static const Color textLight = Colors.grey;
}
